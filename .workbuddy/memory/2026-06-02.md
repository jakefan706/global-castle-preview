# 2026-06-02

## 替换 Manufacturing 工艺图片

- 将 `FACILITY_SLIDES` 从 4 张占位图替换为 6 张工艺实拍图
- 图片来自 `/process/` 目录，用 sharp + mozjpeg 压缩到 500KB 以下
- 压缩脚本：`scripts/compress-process-images.mjs`（二分查找 quality，必要时降分辨率）
- 输出到 `public/images/process/`：check.jpg, injection.jpg, painting.jpg, polishing.jpg, vacuum.jpg, welding.jpg
- 工艺名称：Injection Molding → Welding → Polishing → Painting → Vacuum Process → Quality Check
- 数据文件：`src/app/(frontend)/components/homepage/homepage-data.ts`

## 部署到 staging

- 部署目标：`https://staging.cnurbaneco.com`（server 178.104.6.190，端口 3002）
- 部署流程：rsync → pnpm install → pnpm build → 修复 standalone symlinks → systemctl restart
- SSH key 有 passphrase，通过 `ssh-agent -a /tmp/agent.sock` + `ssh-add --apple-load-keychain` 加载
- 构建成功，TypeScript 通过，服务 active

## 移除工艺图片下方导航点

- 删除 `FacilitySection.tsx` 第 109-121 行的导航点按钮组
- 部署到 staging 验证通过

## 合规认证图标调整

- 10 个认证图标缩小到 80%（容器 190→152px, 图标区 150→120px）
- LFGB 图标：原文件 `LFGB.jpg` 不存在（本地和服务器都没有），从 seeklogo 下载 `LFGB.png`（5.5KB）替换
- 更新 `homepage-data.ts` 中 LFGB 引用路径

## 替换 Explore Our Collections 三张分类图片 (2026-06-09)

- 源图：`/Users/abcd/Vibecoding/website/products/category image/` 下 SS.jpeg、ceramic.jpg、glass.jpg
- 对应：Stainless Steel Series、Ceramic & Stoneware、Glassware Essentials
- 用 sharp + mozjpeg 压缩到 500KB 以下，输出到 `public/images/products/`
  - steel-bottles.jpg: 164KB
  - ceramic-mugs.jpg: 408KB
  - glassware.jpg: 354KB
- 路径不变，原地覆盖旧图；`category-config.ts` 无需修改
