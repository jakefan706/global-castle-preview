# 2026-06-13

## VPS 健康体检自动化
- SSH 到 Hetzner VPS (178.104.6.190) 做了全面健康检查：CPU 0、内存 24%、磁盘 8%、所有服务正常
- 发现 staging Next.js 日志有 `Failed to find Server Action "x"` 报错（每10分钟一次，不影响运行）
- 安装 msmtp 配置了网易企业邮箱 SMTP，但 VPS 出站 SMTP 被 Hetzner 封了（端口 465/587 均超时）
- 改用 **Resend API** 发邮件（域名 cnurbaneco.com 已验证通过）
- Resend API 调用需要带 User-Agent 头，否则 Cloudflare WAF 返回 1010 错误
- 最终方案：VPS 本地 cron 每2天执行一次（每天检查 wrapper 脚本判断是否到间隔），走 Resend API 发邮件
- **脚本位置**：
  - VPS 上: `/usr/local/bin/vps-health-email.py`（主脚本）+ `/usr/local/bin/vps-health-email-cron.sh`（间隔守护）+ `/usr/local/bin/vps-health-check.sh`（数据采集）
  - 本地备份: `scripts/vps-health-email.py`
- **cron**: 每天 10:00 UTC (18:00 CST) 检查，间隔 ≥36h 才实际发信
- **收件人**: sales@cnurbaneco.com
- WorkBuddy 自动化已删除，完全由 VPS 自身驱动
- 邮件标题: 德国VPS 健康体检报告
